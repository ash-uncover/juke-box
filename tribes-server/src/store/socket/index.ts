import * as actions from './socketActions'
import * as reducer from './socketReducer'
import * as selectors from './socketSelectors'
import * as watchers from './socketWatchers'

export {
  actions,
  reducer,
  selectors,
  watchers,
}
